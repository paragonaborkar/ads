import { ArrayFilterPipePipe } from './array-filter-pipe.pipe';

describe('ArrayFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
